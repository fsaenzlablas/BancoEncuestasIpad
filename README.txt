Encuestas version 1

git add <files>

Para hacer commit desde el sh:
git commit -m "Commit Message" -a

Para hacer Push a bitbucket.org: (password Azte-)
git push https://jaimealvarezv@bitbucket.org/jaimealvarezv/bancoencuestas.git